PORT=3001
NODE_ENV=development

# Kubernetes configuration
# The app will try to load kubeconfig from default locations:
# - ~/.kube/config (local development)
# - /var/run/secrets/kubernetes.io/serviceaccount (in-cluster)
# If not found, it will use mock data for demo purposes

